# include <iostream>
using namespace std; 
int main(){
  long long int n, i, temp, m, v, k; 
  cin>>n>>k; 
  if(n%2==0){
    m=n/2;
  }
  else{
    m=(n/2)+1;
  }
  if(k<=m){
     temp=1+((k-1)*2);
  }
  else{
    k=k-m;
    temp=2+((k-1)*2);
  }
  cout<<temp; 
  return 0;
}
